name: Labeler Cron

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  test-run:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/labeler
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
